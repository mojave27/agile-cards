
export const r2d2 = require('../assets/images/r2d2.png')
export const ralph = require('../assets/images/ralph.png')

/*export const importImages = function(r){
    let images = {};
    r.keys().map((item, index) => { images[item.replace('./', '')] = r(item); });
    return images;
}*/